{
  "name": "cloudflare-worker-bluesky-handler",
  "description": "Router based on Hono for Cloudflare Worker implementation in order to change Bluesky's route for DID validations",
  "version": "v1.0.0-250909",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify --env production"
  },
  "dependencies": {
    "hono": "^4.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250906.0",
    "wrangler": "^4.34.0"
  }
}
